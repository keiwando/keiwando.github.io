
var currentProjectFilter="all";const projectFilterButtonAll=document.querySelector('#all-filter');const projectFilterButtonMultiplatform=document.querySelector('#multiplatform-filter');const projectFilterButtonIOS=document.querySelector('#ios-filter');const projectFilterButtonAndroid=document.querySelector('#android-filter');const projectFilterButtonWeb=document.querySelector('#web-filter');const allProjectFilterButtons=[{button:projectFilterButtonAll,filter:'all'},{button:projectFilterButtonMultiplatform,filter:'multiplatform'},{button:projectFilterButtonIOS,filter:'iOS'},{button:projectFilterButtonAndroid,filter:'android'},{button:projectFilterButtonWeb,filter:'web'}];const allProjectLinks=document.querySelectorAll('.projects-grid a');function refreshProjectFilter(){allProjectFilterButtons.forEach(button=>{if(button.filter===currentProjectFilter){button.button.classList.add('selected-filter');}else{button.button.classList.remove('selected-filter');}});}
function refreshVisibleProjects(){allProjectLinks.forEach(project=>{const filters=project.getAttribute('kd:project-filters').split(' ');if(filters.includes(currentProjectFilter)||currentProjectFilter==='all'){project.classList.remove('hidden');}else{project.classList.add('hidden');}});}
refreshProjectFilter();refreshVisibleProjects();allProjectFilterButtons.forEach(button=>{button.button.addEventListener('click',()=>{currentProjectFilter=button.filter;refreshProjectFilter();refreshVisibleProjects();});});